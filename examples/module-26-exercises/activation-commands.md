# Lab 26 — Activation Command Drill

## -D / Maven run (dev)
`mvn -B spring-boot:run -Dspring-boot.run.profile=dev`

## Env activation (your OS → MacOS)
EXPORT SPRING_PROFILES="dev"

## Tests (test profile)
`mvn -B spring-boot:run -Dspring-boot.run.profile=test`

## Scope
Pre-lab only.