# TODO: lab-only teaser — e.g. logging.level.root: WARN
## Document that unrestricted Actuator is not for public prod hosts.
#spring:
#  application:
#    name: northstar-crm
#server:
#  port: 8080
logging:
  level:
    com.northstar.crm: INFO
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never