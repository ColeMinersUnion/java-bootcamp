# WORA Notes

`javac` compiles the Java class into Bytecode. 
When calling the `java` command, it spins up the Java Virtual Machine (JVM)
The JVM is responsible for running the compiled bytecode. 
As such, the JVM does not recompile the source Java class, but rather runs the compiled bytecode.
